# Dejar comentado para desarrollo local con start.ps1 (proxy → Django :8000).
# VITE_API_URL=http://localhost:8000/api

# Con docker compose + Nginx en :80, crea frontend/.env con:
# VITE_USE_NGINX=true
